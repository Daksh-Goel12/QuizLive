import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from 'react-hot-toast'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'QuizLive - Real-time Interactive Quiz Platform',
  description: 'Create and join live quiz sessions with real-time leaderboards and interactive features',
  keywords: 'quiz, real-time, interactive, education, leaderboard, live quiz',
  authors: [{ name: 'Your Name' }],
  openGraph: {
    title: 'QuizLive - Real-time Interactive Quiz Platform',
    description: 'Create and join live quiz sessions with real-time leaderboards',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster 
          position="top-right"
          toastOptions={{
            duration: 4000,
            style: {
              background: '#363636',
              color: '#fff',
            },
            success: {
              duration: 3000,
              iconTheme: {
                primary: '#22c55e',
                secondary: '#fff',
              },
            },
            error: {
              duration: 5000,
              iconTheme: {
                primary: '#ef4444',
                secondary: '#fff',
              },
            },
          }}
        />
      </body>
    </html>
  )
}
